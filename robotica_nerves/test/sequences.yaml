"open_presents":
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup. It is am emergency!
            music:
              play_list: wake_up
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup the adults.
            music:
              play_list: wake_up
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup the adults.
            music:
              play_list: wake_up
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup the adults.
            music:
              play_list: wake_up
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup the adults.
            music:
              play_list: wake_up
    - required_time: "00:05:00"
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to wakeup the adults.
            music:
              play_list: wake_up
    - required_time: "00:30:00"
      latest_time: "01:00:00"
      zero_time: true
      tasks:
        - locations: ["kids"]
          devices: ["robotica"]
          command:
            type: "audio"
            message:
              text: Time to open presents.
