<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>RoboticaFace · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
  </head>
  <body>
    <header>
      <section class="container">
        <nav role="navigation">
          <ul>
          <%= if RoboticaFace.Auth.user_signed_in?(@conn) do %>
            <li><strong><%= RoboticaFace.Auth.current_user(@conn)["name"] %></strong></li>
            <li><%= link "Logout", to: Routes.session_path(@conn, :logout), method: :post%></li>
          <% else %>
            <li><a href="<%= Routes.session_path(@conn, :index) %>">Login</a></li>
          <% end %>
          </ul>
        </nav>
        <a href="/" class="logo">
          Robotica
        </a>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= render @view_module, @view_template, assigns %>
    </main>
    <script type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
